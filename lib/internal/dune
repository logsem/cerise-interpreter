(include_subdirs unqualified)

(library
 (name cerise_internal)
 (package cerise-interpreter)
 (modules
  (:standard \ griotte_extracted))
 (private_modules ir_expression_mapper macro_expander parser_driver)
 (libraries containers menhirLib zarith))

(ocamllex lexer lexer_regfile)

(menhir
 (modules parser parser_regfile)
 (flags --table))
